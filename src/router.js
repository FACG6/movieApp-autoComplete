const {
    handleHome,
    hanleStatics,
    handleAutoComplete,
    handleNotFound
} = require('./handlers');

const router = (request, response)=>{
    const endpoint = request.url;
}

module.exports = router;
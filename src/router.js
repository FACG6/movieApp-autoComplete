const {
    handleHome,
    hanleStatics,
    handleAutoComplete,
    handleNotFound
} = require('./hanlers');

const router = (request, response)=>{
    const endpoint = request.url;
}

module.exports = router;